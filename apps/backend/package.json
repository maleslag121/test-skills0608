{
  "name": "@kaifa-workflow/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "build": "mkdir -p dist && cp -r src/* dist/",
    "start": "node dist/index.js",
    "test": "node --test test/health.test.js",
    "lint": "node --check src/index.js"
  },
  "dependencies": {
    "@fastify/static": "^8.0.0",
    "fastify": "^5.0.0"
  }
}
